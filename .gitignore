node_modules/
dist/

# secrets & env — never commit (keep .env.example only)
.env
.env.*
!.env.example

# local data / db (anchored to repo root — must NOT match src/data/)
*.sqlite
*.sqlite-shm
*.sqlite-wal
/data/

# build/temp artifacts
*.log
*.tar.gz
*.bak
*.bak-*
.mcp_h.txt
.DS_Store
